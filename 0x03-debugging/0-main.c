#include<stdio.h>

/**
 * main - checks the postitibe_or_negative function
 * Return: 0 if the program runs succesfully
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
