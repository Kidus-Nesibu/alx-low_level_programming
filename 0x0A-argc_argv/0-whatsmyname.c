#include<stdio.h>
/**
 * main - prints the argument name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 if success
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	printf("\n");
	return (0);
}
