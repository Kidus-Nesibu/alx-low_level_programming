#include<stdio.h>
#include<stdlib.h>
int main (int argc, char *argv[])
{
	int i;

	if (argc == 1)
	{
		printf("0\n");
	}
	else if (argc >= 65 || argc <= 122)
	{
		printf("Error\n");
	}
	for (i=1; i<argc; i++)
	{
		int sum;
		sum +=atoi(argv[i]);
		printf("%d\n", sum);
	}

	return (0);
